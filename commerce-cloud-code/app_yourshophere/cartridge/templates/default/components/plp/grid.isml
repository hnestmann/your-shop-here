<div class="product-grid">
    <isloop items="${pdict.search.foundProducts}" var="hit">
        <isinclude url="${hit.url}"/>
    </isloop>
</div>
<isprint value="${require('*/cartridge/utils/hookMgr').callHook('wishlist.template', 'refreshState') || ''}" encoding="off" />
