<menu class="flex">
    <isloop items="${pdict.root.children}" var="menuItem">
        <isset name="level" value="${1}" scope="page" />
        <isinclude template="components/header/menuitem" />
    </isloop>
</menu>
